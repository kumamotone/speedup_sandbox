import 'package:flutter/material.dart';

class GenericLoadingView extends StatelessWidget {
  const GenericLoadingView({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("error"),
    );
  }
}
